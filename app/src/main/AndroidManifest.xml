<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application
        android:name=".ui.notification.BaseNotification"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScreenOffTimer"
        android:enableOnBackInvokedCallback="true"
        android:forceDarkAllowed="false"
        tools:targetApi="33">
        <activity
            android:name=".MainActivity"
            android:forceDarkAllowed="false"
            android:theme="@style/Theme.ScreenOffTimer.SplashScreen"
            android:configChanges="locale|layoutDirection"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--        <receiver-->
        <!--            android:name=".MyDeviceAdminReceiver"-->
        <!--            android:exported="true"-->
        <!--            android:permission="android.permission.BIND_DEVICE_ADMIN">-->

        <!--            <meta-data-->
        <!--                android:name="android.app.device_admin"-->
        <!--                android:resource="@xml/device_admin_receiver" />-->

        <!--            <intent-filter>-->
        <!--                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />-->
        <!--            </intent-filter>-->
        <!--        </receiver>-->

        <service
            android:name=".service.TimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse|dataSync|mediaPlayback"
            android:stopWithTask="false" >
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="function for timer countdown for off the screen" />
        </service>

        <service
            android:name=".service.MediaControlAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            tools:ignore="AccessibilityPolicy">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_config"
                android:value="2.1" />
        </service>

        <receiver android:name=".core.broadcast.TimerReceiver" />
    </application>
</manifest>